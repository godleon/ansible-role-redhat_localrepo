server {
    listen       {{ redhat_localrepo.web_server_port }};
    server_name  localhost;

    root   {{ redhat_localrepo.web_root_dir }};

    location / {
        autoindex on;
        autoindex_exact_size off;
        autoindex_localtime on;
    }
}